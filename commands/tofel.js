const { execute } = require("./sasageyo");

module.exports = {
    name: 'tofel',
    description: "sends tofel quotes",
    execute(message, args){
        message.channel.send(`TOFEL KE DISCORD CUMAN NYUMBANG BATUK`);
        }
    }